name: wo_a
version: '$version'

main: me.unkor.wo_a.Wo_a
api-version: '1.21.11'
load: POSTWORLD

commands:
  woa:
    usage: /woa <apply|give> <player>
    permission: permission.admin

permissions:
  permission.admin :
    description: "This permission for admins"
    default: op
    children:
      permission.admin : true